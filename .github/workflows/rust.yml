name: Rust

on:
  push:
    branches: [ "src" ]
  pull_request:
    branches: [ "src" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: kali-linux

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --release 
